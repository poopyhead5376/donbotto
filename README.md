# donbotto

A retro-inspired landing UI for **Donbotto**, a free Linux desktop automation concept that uses local/open LLMs and executes actions with `xdotool` in real time (instead of JSON action plans).

## Run locally

```bash
python3 -m http.server 4173
```

Then open <http://localhost:4173>.
